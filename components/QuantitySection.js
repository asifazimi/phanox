import { useProductContext } from "../lib/context";

const QuantitySection = () => {
  const { increaseQuantity, decreaseQuantity, quantity } = useProductContext();

  return <></>;
};

export default QuantitySection;
